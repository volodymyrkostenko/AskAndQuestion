<% unless current_user.is_a? GuestUser %>
  <%= form_for @answer, url: '#' do |f| %>
    <div class="form-group">
      <%= f.text_area :body, class: 'form-control' %>
    </div>

    <%= f.submit 'Post Answer', class: 'btn btn-primary' %>
  <% end %>
<% end %>